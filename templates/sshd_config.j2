# Managed by Ansible (0xOI/ansible-role-openssh)
# see: sshd_config manual -- https://man.openbsd.org/sshd_config

{% for key, value in ssh_config.service.items() %}
{{ key }} {{ value }}
{% endfor %}
