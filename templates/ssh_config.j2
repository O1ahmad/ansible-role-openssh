# Managed by Ansible (0xOI/ansible-role-openssh)
# see: ssh_config man specification -- https://man.openbsd.org/ssh_config

{% for spec in ssh_config.client[account] %}
{{ ssh_config.client[account][spec].keyword|default("Host", true) }} {{ spec }}
{% for key, value in ssh_config.client[account][spec].options.items() %}
  {{ key }} {{ value }}
{% endfor %}

{% endfor %}
